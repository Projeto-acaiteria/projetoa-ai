# Starteq (ComandaPRO) — Estado de Desenvolvimento / Handoff

_Última atualização: 13/07/2026. Escrito pra uma NOVA instância do Verbo continuar o desenvolvimento sem perder contexto._

## O que é
- **Starteq** = tenant de Assistência Técnica (loja de informática/games) DENTRO do ComandaPRO. Não é app separado: é a plataforma multi-tenant multi-vertical rodando em `comandapro.net.br/starteq`.
- **Site** = `starteq-palmas` (repo separado, `github.com/ImpulsoDigital063/Starteq` → `starteq.vercel.app`) = VITRINE headless que lê tudo do ComandaPRO. Projeto vendido à parte pro Junior (dono da Starteq); contrato ainda NÃO fechado — estamos montando o pacote pra vender a ideia.
- Ver memórias: `reference_starteq_tenant_comandapro`, `project_comandapro_saas_multivertical`, `project_starteq_caixa_comissao_modelo`.

## Infra / comandos
- **Repos:** ComandaPRO = `C:/Users/Usuario/acai-system` (org `Projeto-acaiteria`) · Site = `C:/Users/Usuario/starteq-palmas` (org `ImpulsoDigital063`). **PUSH DOS DOIS = conta gh `ImpulsoDigital063`** (`gh auth switch --user ImpulsoDigital063`). ⚠️ **AppDelyvery NÃO tem acesso a nenhum dos dois** (dá 403) — nunca deixar AppDelyvery ativa pra pushar; e NÃO rodar `gh auth setup-git` (sobrescreve credencial boa). λ 3-contas-gh.
- **Deploy:** ambos auto-deployam `main` na Vercel. ComandaPRO `main` → `comandapro.net.br` (team projeto-acaiterias). Site `main` → `starteq.vercel.app`.
- **⚠️ FOOD PAGANTE NO MESMO DEPLOY:** Cantinho do Açaí e Medellín são tenants no MESMO app/deploy. Slug isola DADOS, não CÓDIGO. Toda mudança em arquivo compartilhado (AdminShell, globals.css, orders-store, settings-store, layout) atinge eles. SEMPRE: análise de impacto + branch + read-after-deploy (`curl` Cantinho dourado `F4C95C` / Medellín vermelho `FF3B4E`). NUNCA quebrar food.
- **Starteq store_id:** `c1251296-9bee-40ed-a98e-472614c61bf8`. Slug `starteq`.
- **Logins teste:** dono `starteq@teste.com` / `Starteq2026` · técnico `tecnico@starteq.teste` / `Tecnico2026` (staff Rafael Costa 30%; + Marina Lopes 25% criada na simulação) · **recepção `recepcao@starteq.teste` / `Recepcao2026`** (store_members role=reception, criado 07/07).
- **DB read (SQL):** `node scripts/query.mjs "SELECT ..."` (pg, prod).
- **Rodar a LÓGICA REAL do app por script (test harness):** `npx tsx --env-file=.env.local scripts/X.ts` — chama as store functions de verdade (totais/baixa/comissão/cupom reais). **REGRA DURA: passar `storeId=Starteq` EXPLÍCITO em toda chamada** — sem isso `resolveStoreId()` cai no Cantinho (fallback). Projeto é CJS → usar `async function main(){...}; main()`, sem top-level await, sem BigInt literal.
- **Login por navegador tem captcha** (Supabase) → agente não loga sozinho. Testes de UI: CIC com o Eduardo já logado, ou ele mesmo.
- **Coordenação:** existe outra instância "Verbo do ComandaPRO" que também mexe na `main` do acai-system. Antes de merge, `git fetch` + conferir sync; combinar "trava a main".

## Estado DEPLOYADO (tudo em prod, main)
ComandaPRO topo — conferir `git log` (13/07 subiu o módulo de comissão+caixa) · Site topo `71a5b48`.

**Shipped 13/07/2026 — Check-in por CPF + comprovante 80mm de entrada + importador de clientes (estudo do GestãoClick):**
- **Contexto:** o GestãoClick (ERP+loja da Starteq) identifica cliente por CPF na recepção e imprime comprovante. Estudamos o concorrente (relatório completo no chat/Verbo) e trouxemos a paridade.
- **Migrations ADITIVAS (prod, verificadas):** `service_orders.cpf text` + tabela `service_customers (store_id, cpf, data jsonb, PK(store_id,cpf), RLS on)` — base de clientes por CPF, service-only. Scripts `scripts/migrate-clientes-starteq.mjs` (idempotente).
- **`service-customers-store.ts`:** `getServiceCustomerByCpf`, `upsertServiceCustomer` (best-effort). `service-orders-store`: OS ganhou `cpf`; `createServiceOrder` alimenta a base por CPF; `lookupCustomerByDoc(doc)` = base de clientes 1º, OS antiga como fallback.
- **`/api/os`:** GET `?doc=<cpf>` (busca cliente recorrente, bloqueado p/ técnico); POST create aceita `cpf` e devolve `{os, store}` (cabeçalho da loja p/ imprimir).
- **Check-in (`NovaOSButton`):** campo **CPF + botão "buscar"** (acha cliente e auto-preenche nome/telefone) + imprime **comprovante 80mm de entrada** ao abrir a OS (`osEntryTicketHtml` em `ticket.ts`: header white-label + nº OS + cliente/CPF/tel + aparelho/defeito + **garantia + cláusula de abandono Art.1.275** + assinatura; SEM marca de terceiro, ≠ GestãoClick que assina "emitida no GestãoClick"). Gate impressão `autoprint:os !== "0"`.
- **Importador de clientes:** `scripts/import-clientes-starteq.mjs` — lê CSV exportado do GestãoClick (Clientes > "Mais ações" > Exportar cadastros), autodetecta `;`/`,`, mapeia colunas (nome/cpf/telefone/email/endereço), `--dry` pra conferir, `--latin1` p/ Excel BR, upsert idempotente por CPF. **Espera só o CSV do Junior.** Alternativa: API do GestãoClick (token, add-on) expõe clientes por GET.
- **Validado:** tsc limpo + build OK + parser testado (aspas/vírgula/sem-CPF). **Limite honesto:** a busca por CPF só acha quem já está na base (importada OU de check-in nosso) — não puxa a base antiga do GestãoClick sem o export.

**Shipped 13/07/2026 — Módulo Orçamento + Documento A4 público + WhatsApp (paridade GestãoClick):**
- **Migration:** tabela `budgets` (jsonb, service-only, RLS on) + índice em `data->>'code'`. `scripts/migrate-orcamentos.mjs`.
- **`budgets-store.ts`:** Budget com itens de linha (`kind` produto/serviço, qty, unitCents, discountCents), frete/outros/desconto global, validade, status (pendente/aprovado/recusado/expirado). Helpers `itemSubtotalCents`/`budgetTotals`. CRUD + `getBudgetByCode` (público). `service-orders-store.getServiceOrderByCode` (OS pública por código).
- **Documento A4 PÚBLICO `/doc/[code]`** (`app/doc/[code]/page.tsx` + `components/DocA4.tsx` + `PrintButton`): resolve orçamento OU OS pelo código e renderiza um A4 branded (header da loja, cliente, equipamento [OS], tabela de itens peça/serviço, totais, garantia + validade/abandono, assinaturas). **Sem marca de terceiro** (≠ GestãoClick). Imprime / "salvar PDF" pelo navegador. Sem login.
- **API `/api/orcamentos`** (guard: service + owner/reception) + tela **`/admin/orcamentos`** (`OrcamentosClient`): criar/editar com **itens de linha** (peça/serviço + qtd/valor/desconto), CPF+buscar cliente, frete/outros/desconto/validade/obs, total ao vivo; lista com status, **Ver A4**, **Enviar por WhatsApp** (wa.me + link /doc/code), Aprovar/Recusar, Editar, Excluir.
- **OS ganhou compartilhamento:** `components/admin/DocShare.tsx` na `/admin/os/[id]` (Ver A4 + WhatsApp). Nav "Orçamentos" (service) no AdminShell; recepção liberada em `roles.ts`.
- **Fase 2 (FEITO 13/07):** aprovar orçamento **gera a OS automática** (`createOSFromBudget`: serviços→service_value, peças+frete+outros→parts_value, desconto→discount; os_parts sem SKU). Trava anti-duplicação (`budget.osId`), idempotente. UI: "Aprovar → gerar OS" + link "OS gerada: CÓDIGO". API action `aprovar`.
- **Dados da loja no A4 (FEITO 13/07):** `StoreSettings` ganhou `email`/`site`/`responsavel` (getStore faz passthrough; setStore sanitiza). DocA4 mostra cnpj/endereço/tel(formatado)/email/site/responsável. **Dados reais da Starteq preenchidos** via `scripts/set-starteq-dados.mjs` (CNPJ 28.623.696/0001-21, end. Plano Diretor Sul, (63)99252-8619, starteqpalmas@gmail.com, Responsável Junior Falcão). O 80mm também passa a ter o cabeçalho completo. **Confirmar com Junior:** site (usei starteqpalmas.com; o GestãoClick deles lista www.starteq.com.br).
- **Fase 3 (FEITO 13/07):** (a) **"expirado" automático** — `withExpiry` em budgets-store marca orçamento pendente vencido como expirado (só exibição, aprovar ainda funciona). (b) **Ajustes editável** — ConfigClient ganhou seção "Documento de OS/Orçamento" (gated service): email, site, responsável, **Termos de garantia** (textarea, 1 cláusula/linha) e **Observações/avisos**. Página passa `family`. API já salvava (setStore sanitiza). Também `garantiaTermos`/`avisos` viraram settings com DEFAULT completo (getStore fallback). (c) **Documento A4** agora mostra seções "Termos de garantia" (OS+orçamento) e "Observações" (abandono, só OS) + dados da loja no cabeçalho (cnpj/end/tel/email/site/responsável).
- **Refino do documento A4 (13/07):** modelo ENQUADRADO (boxes/barras) igual GestãoClick; **fix impressão em branco** (o `globals.css` esconde tudo menos cupom 80mm → `.wrap` do doc vence por especificidade); dados da loja preenchidos + **logo** (`store.logoUrl`) no cabeçalho; mensagem de WhatsApp cita a loja pelo nome (DocShare/OrcamentosClient recebem `storeName`).
- **Fase 4 — obs impressa × interna (FEITO 13/07):** `service_orders.print_obs` (observação que SAI no documento, recepção/dono edita) — capturada no check-in (NovaOSForm), editável na OS (`OSObsEditor`), aparece no doc como "Observações". As `notes` do técnico continuam INTERNAS (nunca no doc). A seção legal virou "Avisos importantes" (pra não colidir). Ação `/api/os` "printObs" (fora de TEC_ACTIONS → técnico não escreve).
- **A fazer (fase 4 restante):** converter orçamento→venda (itens texto livre = sem baixa de estoque automática — precisa SKU-linking); PDF server-side (deixado de lado: navegador já salva PDF, WhatsApp não anexa arquivo).
- **Check-in de OS = PÁGINA (não modal):** `/admin/os/nova` (`NovaOSForm`) com seções Cliente/Equipamento/Serviço (familiaridade GestãoClick); "+ Nova OS" virou link. Fim do modal transparente.
- **Familiaridade (13/07):** editor de **Orçamento** virou página `/admin/orcamentos/editor` (novo e `?id=` edição), seções Cliente/Itens/Valores/Observação; lista ficou só com os cards. **Painel de OS** `/admin/os/painel` (inspirado no "Painel" do GestãoClick): tiles de **Prazos** (via `estimated_at`), **Prioridade**, **Situação** e **Faturamento por técnico** (barras, OS quitadas). Link "Painel" no header da lista.
- **Compras / reposição (13/07):** módulo novo `/admin/compras` (+ `/api/compras`, `purchases-store.ts`, tabela `purchases` service-only RLS). Pedido de compra (fornecedor + itens ligados ao produto do estoque via datalist + frete). **Receber** → `moveStock("entrada")` nos itens com SKU + atualiza custo (`updateItem costCents`) + gera despesa (`addExpense` categoria pecas). Idempotente (não recebe 2x). Guard service + owner/reception; nav "Compras". Base pro gap "compras/reposição". Item avulso (sem SKU) não mexe no estoque.
- **Campos novos na OS (13/07):** **técnico** escolhível no check-in (puxa o % de comissão dele); **Prioridade** (`priority`, `os-priority.ts` módulo puro — muito_urgente..baixa) no check-in + OSActions + badge na lista + seção no painel; **Marca/Modelo** (`marca`/`modelo`) na seção Equipamento do check-in, exibidos na OS e no doc A4. (Solução=nosso Laudo/`diagnosis`; Termos=garantia da loja.) Ações `/api/os`: `priority` (fora de TEC_ACTIONS). Migrations aditivas todas aplicadas.
- **Código de barras no balcão (13/07):** campo `barcode` (EAN) no `StockItem` (jsonb — sem migration); input no cadastro/edição de Estoque (só itens vendáveis) e persistência em `/api/estoque` (POST + PATCH). Tela **Vendas**: leitor de barras no topo do catálogo — bipa o EAN (leitor digita + Enter) → índice `byBarcode` acha o produto e joga no carrinho, com feedback "+ nome" ou "não cadastrado". `autoFocus` no campo pra bipar direto.
- **Validado:** tsc limpo + build OK (rotas /admin/orcamentos, /api/orcamentos, /doc/[code]). Food intacto.

**Shipped 13/07/2026 — OS enriquecida (teardown de UI/UX do GestãoClick no trial):**
- Estudei o sistema do GestãoClick por dentro (trial do Eduardo, via Chrome). Achados: OS deles é página multi-seção pesada (Dados gerais/Responsáveis/Equipamentos com Condições+Defeitos+Acessórios+Solução/Produtos-Peças e Serviços-Mão de obra em tabelas de linha/Totais com Frete+Outros/Anexos/Obs impressa×interna). Financeiro = livro-caixa mensal com plano de contas + export Excel. Visual AdminLTE datado. **Nenhum dos dois tem garantia estruturada** (oportunidade).
- **Trazido pra cá (colunas aditivas na prod):** `service_orders.condicoes` + `service_orders.acessorios`. Capturados no **check-in** (NovaOSButton), impressos no **comprovante 80mm** e exibidos na OS (`/admin/os/[id]` e `/admin/minha-area/[id]`). Protegem contra disputa ("entregou trincado / com carregador"). Nosso "Solução" = `diagnosis` já existia.
- **A fazer (backlog do teardown):** produtos/serviços como tabelas de linha (item+qtd+valor+desconto+subtotal) p/ orçamento/nota; obs impressa×interna; garantia estruturada com data de expiração (supera os dois); plano de contas no financeiro; export Excel.

**Shipped 13/07/2026 — Pagamento de comissão + comissão no caixa (estudo do Palace portado):**
- **Migration ADITIVA (já em prod, verificada):** `service_orders.commission_payment_id text` (NULL=comissão pendente, preenchido=paga) + tabela `commission_payments` (jsonb `data`, RLS ligada). Script `scripts/migrate-commission-caixa.mjs` (idempotente, read-after-write). **Food intacto:** `service_orders` é só do vertical service; `commission_payments` é nova.
- **Store `src/lib/commission-payments-store.ts`:** `payCommission` (recalcula no server via `osCommissionCents`, **trava anti-2x** = só vincula OS com `commission_payment_id IS NULL`, rollback total se corrida), `pendingOSForStaff`, `listCommissionPayments`, `reverseCommissionPayment` (estorno devolve OS pra pendente). Modelo espelhado do Palace (`commission_payments` + FK na OS).
- **`/api/equipe` estendido:** GET report agora traz `pendenteCents/pendenteCount` (all-time não pago) além do período; `?staff=<id>` retorna OS pendentes + histórico (base do wizard); POST ações `payCommission` e `reverseCommission`. Guard 3 camadas mantido (auth + family=service + owner).
- **UI `/equipe` (`EquipeClient`):** card do técnico mostra **Pendente (a pagar)** + botão **"Registrar pagamento"** → modal com OS pendentes (pré-marcadas), **pagamento parcial**, **bônus + motivo**, data, obs., e **histórico com estornar**.
- **`/minha-area` do técnico:** stats agora separam **"A receber (pendente)" × "Já recebido"** (usa `os.commissionPaymentId`); label da OS quitada = "recebida"/"a receber".
- **Caixa — comissão como despesa (aprendizado Palace: sem isso o lucro fica falso-alto):** `/api/financeiro` devolve `comissoes` (saídas sintéticas por `paidAt`: categorias `comissao`/`bonus`; só service tem, food volta vazio). `FinanceiroClient` soma essas comissões nos TOTAIS/gráficos/fluxo (resultado real) mas **não** na aba Despesas (não editáveis). O **fluxo dia/semana/mês/ano JÁ EXISTIA** (`FluxoCaixaTabela`, saldo encadeado + drill + CSV + taxa maquininha como despesa) — só faltava a comissão entrar.
- **Categorias de despesa por family:** `financeiro/page.tsx` passa `family`; service usa Peças/Frete/… (food mantém Insumos/Embalagens). Rótulos `pecas/frete/comissao/bonus` em `FinanceiroClient` e `FluxoCaixaTabela`.
- **Validado:** `tsc --noEmit` limpo + `next build` OK. Prova-na-fonte: Starteq tem 6 OS quitadas c/ técnico pendentes (coluna nova NULL). **NÃO testado E2E logado** (captcha) — Eduardo/CIC precisa: pagar comissão de teste → some do pendente → aparece no histórico → vira despesa no fluxo de caixa.

**Shipped nesta sessão (06/07, provado em prod):**
- `5a6f0b8` **Caixa × OS (decisão B):** OS quitada DENTRO da sessão entra no Caixa. Só o DINHEIRO da OS soma no `saldoCaixaCents` (gaveta física); pix/cartão de OS aparecem no total mas não incham a gaveta (igual às vendas). Nova MiniStat "OS quitadas" (some pro food). Snapshot do `closeCash` guarda `osCashCents`/`osTotalCents`. Núcleo em `src/app/api/caixa/route.ts` `resumo()` + `cash-store.ts`. Provado CIC end-to-end: abriu caixa (0) → quitou OS dinheiro R$3.148,90 → saldo pulou exato. **Pendência de polimento (opcional, ok do Eduardo):** MiniStat "Dinheiro" (vendas balcão) fica R$0 enquanto gaveta tem o dinheiro da OS — renomear p/ "Dinheiro (vendas)" se quiser.
- `31f49a3` **Tela do técnico completa (bancada):** `/admin/minha-area` virou fila agrupada (A fazer/Em reparo/Prontas) + nova página tech-scoped `/admin/minha-area/[id]` (aparelho/peças/comissão-dele + `TecOSWork`: status de bancada, editor de laudo, fotos antes/depois com `compressImage` reusado). **Migration:** `service_orders.photos jsonb default '[]'` (JÁ aplicada em prod). Store: `updateOSDiagnosis`/`addOSPhoto`/`removeOSPhoto`. **`/api/os` FECHADO por papel** (era aberto — furo): técnico só status/laudo/foto na OS DELE, status só bancada; nunca quitar/assign/create. Provado CIC: status+laudo gravam. **Foto via CIC não testada (precisa arquivo real)** — Eduardo testa com foto de aparelho de verdade. Deixei laudo demo na OS MPXXX2.
- `0d90cb6` **Bancada v2:** WhatsApp do cliente (wa.me) + "entrou há X dias" (na OS e na fila); seção **Entregues** (histórico) + "você já ganhou R$X"; **senha do aparelho** (capturada no check-in NovaOSButton, mostrada pro técnico) — coluna `device_password` já existia; **anotações de bancada** (`notes`, editável pelo técnico, separado do laudo; action "notes" nas permitidas do técnico). Store: `updateOSNotes`; `NewOSInput.devicePassword`. Provado CIC: senha/tempo/anotações renderizam e salvam. MPXXX2 = demo cheia (senha 1234 + laudo + notes). Falta rodada dedicada p/ "registrar peça usada" (deferido pelo Eduardo — ele decidiu NÃO fazer: peça é da recepção/dono no check-in/montagem).
- `919bef8` **Prazo estimado:** técnico define quando fica pronto (`estimated_at`, guardado no FIM do dia BR 23:59:59-03:00 p/ só virar atrasada depois do dia passar). Badge vermelho "⚠ atrasada" na OS e na fila quando venceu e a OS ainda está ativa. Store `updateOSEstimate` (YYYY-MM-DD→ISO ou null); action "estimate" nas permitidas do técnico. Roundtrip de fuso provado (harness) + render/atrasada no CIC. **Gotcha CIC:** input `type=date` nativo é chato de digitar via automação — testar setando via SQL + screenshot, ou o Eduardo digita.
- `22362ef`+`b4df633` **Estoque AT-aware (prep pra sincronização site↔sistema):** o site tem modelo rico (specs por categoria) que alimenta o montador; o editor do ComandaPRO era food-only → produto novo entrava sem specs e quebraria o montador. Agora o sistema é a fonte gerenciável. **`config/at-specs.ts`** = CONTRATO de specs por categoria (config do VERTICAL AT, compartilhada por todo tenant service; food não usa — confirmado por Eduardo que Starteq nasceu como AT, não food). `StockItem` +badge/highlight/image (specs/brand já existiam). `AtSpecsFields` (inputs guiados). `EstoqueClient/AddModal` AT-aware **gated `family==="service"`** (food IDÊNTICO): categoria AT → campos de spec + marca + preço (hardware é vendável) + selo/destaque + **foto real** (`ImageUpload`→Storage→site, resolve imagens junto). **Fix `b4df633`:** whitelist `CATS` do POST /api/estoque aceitava só food → cat AT virava "outro"; adicionadas as 15 cats hardware (pego no teste CIC). Provado CIC (dono): specs persistem (`{socket:AM5,tdp:105,cores:12}`). **FALTA:** (a) EditModal AT-aware (editar specs de existente — só fiz Add); (b) esconder campos food (Compra em/dose) no form service; (c) periféricos sem contrato de specs ainda; (d) plugar `image` no getProducts do site (hoje usa SVG). **gh: push com ImpulsoDigital063 (ver Infra).**
- `d4983f7` **Offline — motor de fila (resiliência a quedas), Increment 1 (check-in):** capacidade de PLATAFORMA (`src/lib/offline-queue.ts` — IndexedDB cru, sem dep; `submitOrQueue` envia se online / enfileira se offline; `flushQueue` drena ao reconectar — rede:para, 4xx:descarta, 5xx:mantém; erro de SERVIDOR não enfileira). `OfflineIndicator` (barra conexão + contador pendentes + sync no evento "online"). **ATIVADO só no vertical service (Starteq)** via gate `nav.family==="service"` no `(protected)/layout.tsx` — **Cantinho/Medellín (food) NÃO recebem** (não é cobaia; entram depois com impacto/staging, per [[starteq-offline-pwa-resiliente]]). `NovaOSButton` usa o motor (online provado CIC: OS criou; offline avisa "salvo local"). SW/manifest já eram app-wide (leitura offline). **Falta:** Increment 2 = fluxos de dinheiro offline (venda/pagamento/fechar caixa) com "provisório até sync". **Teste offline:** DevTools→Network→Offline (não dá CIC/harness — IndexedDB).
- `3eb291a`+`461175f` **Melhorias do cockpit da recepção (A-D):** **A** busca de balcão (`BuscaOS` + GET `/api/os?q=`, `searchServiceOrders` ilike code/nome/tel, sanitizado anti-injeção). **B** prontas com "pronta há X dias" (`ready_at`, carimbado no →pronto/limpo no reparo) + "✓ avisado" (`notified_at`, carimbado ao abrir WhatsApp; action "notify"). **C** atribuir técnico (`AtribuirTecnico`) na OS aguardando + confirmar pedido do site (reusa `PedidosPendentes`) inline no cockpit. **D** painel "precisa de atenção" (OS atrasadas + prontas encalhadas 3+ dias + pedidos parados). **Migration `ready_at`+`notified_at` aplicada em prod.** Tudo provado CIC (recepção logada) + harness. Demo populada: 4SGXF7 encalhada 4d, A5XTGH atrasada, MPXXX2 avisado (dados de teste p/ Tarefa 4).
- `b0a5a1c` **Cockpit da RECEPÇÃO (Início papel-aware):** recepção agora cai num Início de OPERAÇÃO (não no dashboard do dono) — SEM o faturamento da loja (ela é bloqueada do Financeiro). `AdminHome`: se `role==="reception"` → `ReceptionHome`, senão `ATHome`. Cockpit: caixa abrir/fechar, ações (Nova OS/check-in, Nova venda, Montar PC, Ver OS), Prontas p/ retirada, Pedidos do site, Aguardando, Em reparo. **Entregar OS:** `/api/os` action "entregar" (se não quitou, cobra agora c/ forma + baixa, depois entregue; já quitada só entrega — NÃO é ação do técnico). **Avisar cliente:** botão WhatsApp c/ msg pronta (`RecepcaoProntaAcoes`). Provado CIC (recepção logada): cockpit renderiza, Entregar funciona (OS→entregue), Montador acessível. `entregar` provado por harness.
- `2a08c1a` **Guard server-side por papel:** `src/lib/auth/guard.ts` `requireNavAccess(href)` em TODAS as 23 páginas do `(protected)`. Espelha a matriz `canSeeNav` (fonte única: URL-access == visibilidade no nav). Técnico/recepção não furam mais por URL. Custo ~zero (getCurrentRole é cache() e o layout já chama). Food = no-op p/ owner. Provado: matriz 9/9 + CIC owner (sem regressão, minha-area redireciona) + técnico (financeiro/os → minha-area). Redirect: técnico→minha-area, demais→/admin.

1. **Vertical AT em prod:** OS (service_orders/os_parts), check-in, montador de PC (`pc-builder.ts`), detalhe/status/quitar (comissão + baixa), Minha área do técnico, papéis (owner/recepção/técnico via `roles.ts` + `store_members`), cupons. Marca P&B (default neutro).
2. **B — Vendas de peça** (`/admin/vendas`, family "service", recepção liberada): venda de balcão vira `Order` mode "balcao" → cai no Financeiro/caixa, NUNCA gera comissão (comissão só em service_orders). Núcleo `src/lib/parts-sale.ts` (`createPartsSale`).
3. **Pedido do site:** `POST /api/loja/[slug]/pedido` (público+CORS) cria pedido "recebido"; `POST /api/loja/[slug]/montagem` cria OS de montagem. OS nasce com `code` rastreável.
4. **Fase 2 — balcão confirma pedidos do site:** seção "Pedidos do site" no topo de Vendas → confirmar (vira venda + baixa) ou cancelar. `/api/at-pedidos` (confirm/cancel), `confirmPedido`/`cancelPedido` em parts-sale.
5. **Financeiro × OS:** `/api/financeiro` soma orders + mesas + **OS quitadas** (antes OS só aparecia no dashboard).
6. **Desconto PIX + maquininha/parcelas:** `StoreSettings.pixDiscountPercent` (Adm define em Ajustes). `createPartsSale`/`confirmPedido` aplicam desconto PIX (cliente paga menos) + taxa de máquina por parcela (custo da loja, reusa `resolveCardFee`). Catálogo (`/api/loja/[slug]/produtos`) retorna `pixPriceCents`. UI: campo % PIX em Ajustes + seletor máquina/parcelas em Vendas. Site mostra o preço PIX descontado real.
7. **Site headless:** vitrine/produtos/categorias/produto/montador leem do ComandaPRO (`src/lib/comandapro.ts`, default `https://comandapro.net.br`, `.env.local`=localhost no dev). Carrinho → `postPedido`.

## Testado
- **CIC E2E (06/07):** cliente compra no site (carrinho→pedido #2191/UVHM4N + montador→OS) chegam no painel; operador confirma/cancela/vende/quita — tudo OK. 3 achados: financeiro×OS (corrigido), montador sem código (corrigido), card "não clicável" (falso positivo).
- **Simulação de integridade densa (tsx, 06/07):** 17 operações (6 OS quitadas 2 técnicos, 7 vendas dinheiro/pix/débito/crédito-12x/2 cupons/1 estorno, 3 pedidos). Veredito `✓ INTEGRIDADE OK`: estoque baixa certo + estorno reverte, PIX 5% ok, taxa 12x=6% ok, comissão por técnico só service_value de OS quitada, cupons contabilizam, cancelamento sem vazamento.
- **Financeiro mostra líquido, não cheio** (auditado): todas as telas somam `totalCents` (com desconto), nunca `subtotalCents`. Ver `feedback_financeiro_mostra_liquido_nao_cheio` (erro que travou o Palace — NÃO repetir).

## PUNCH-LIST DE UI/UX (07/07 — feedback do Eduardo em 3 telas)
Eduardo apontou 3 telas do painel do dono/recepção. Estado:
- **OS (`/admin/os`) — ✅ FEITO** (`1e521c4`): era lista plana; agora `OSListClient` com **busca** (código/cliente/aparelho) + **abas por status** (Em aberto/Aguardando/Em reparo/Prontas/Entregues/Todas) + "entrou há X dias". AT-only.
- **Vendas — ✅ FEITO** (`28ba542`): "Pedidos do site" virou **banner colapsável** (default fechado, contador) → o PDV de produto (busca+grid+carrinho) fica o foco. AT-only.
- **Caixa (PDV) — PARCIAL:** ponte feita (`413c81a`): no service, caixa aberto mostra **"Vender no balcão" + "Montar PC"** (era "use Balcão/Mesas", que não existe no AT). **FALTA a decisão do Eduardo (a/b):** (a) Caixa vira a ÚNICA tela — embutir o PDV de produto (VendasClient) DENTRO do caixa, Vendas some; (b) manter Caixa+Vendas separados mas ligados (estado atual). VendasClient já é PDV completo (busca+grid+carrinho+pagamento via /api/vendas-peca) — reusar no embed. CaixaClient é SHARED com food → gate `family==="service"`.
- **Impressora/QZ — DIAGNOSTICADO, fix travado:** o diálogo "Medellin Music Bar" = CN do **cert-mãe compartilhado** (`qz-cert.ts`, "mesmo do Medellin"). 3 causas: QZ Tray app fechado (local) · override.crt não instalado (local, dá o Allow) · nome cosmético. **Fix (#11) = gerar cert neutro "ComandaPRO" + redistribuir override.crt em TODAS as máquinas INCL. Medellín (food pagante ao vivo) → coordenado, BLOQUEADO até Eduardo combinar com o Medellín.**
- **Comanda de entrada / avisar cliente (#13) — NÃO FEITO:** avisar WhatsApp na OS = rápido (não precisa impressora); comanda impressa = depende do QZ (item acima). Fica pra retomar.

**PRÓXIMO FOCO (Eduardo, fim da sessão 07/07):** DESIGN DO SITE (`starteq-palmas`) — a próxima frente é mexer no design/visual do site, não mais o sistema.

## PRÓXIMOS PASSOS (onde parei)
1. ~~**Testar TODAS as rotas**~~ ✅ FEITO (06/07). Públicas via curl (produtos/pedido/montagem OK, provado no banco). Dados admin conferidos no banco (OS/orders/estoque/cupons sãos, comissão exata). Financeiro CONSOLIDA OS (o "△" do outro Claude era o Caixa, não o Financeiro — falso positivo). Descoberta: cash/expense/customer functions não aceitam storeId → não harnessar (cai no Cantinho).
2. ~~**Papéis técnico/recepção — guard server-side**~~ ✅ FEITO + provado (commit `2a08c1a`, ver acima).
3. **Limpeza dos dados de teste:** Starteq tem o dia simulado (10 vendas + 8 OS quitadas + máquina/PIX) + testes CIC. **AGORA TAMBÉM: caixa ABERTO (fundo 0, desde ~19:14) + OS `2YUFNA` quitada em dinheiro (R$3.148,90) + pedido `#2215/PPRX8E` recebido — tudo marcado "TESTE VERBO", da prova comportamental.** DECISÃO PENDENTE do Eduardo: limpar e zerar, ou deixar como demo. Config: PIX 5%, máquina "Stone" (déb 1.5/créd 3.5/parc 6.0).
### Conceitos colhidos do "ERP-fantasma" do site (antes de removê-lo, 07/07)
O `starteq-palmas` tinha um ERP-protótipo INTEIRO em mock (`admin-mock.ts` 800 linhas + `/admin/*` 38 telas) — o rascunho do que viramos real no ComandaPRO. Removido (repo do site), mas estas IDEIAS valem portar pro ComandaPRO:
- **Fiscal AT (base pro Focus NFe):** `osFiscalBase` separa **NFSe** (ISS · mão de obra = `service_value − desconto_serviço`) de **NFCe** (ICMS · peça = `parts_value − desconto_peça`). Comissão NUNCA usa base fiscal — é sobre `service_value` BRUTO. Split serviço×peça é o coração do fiscal de assistência.
- **Importar dados antigos (migração do Junior):** o site tinha import CSV de **clientes** (`modelo-clientes-starteq.csv`) e **OS** (`modelo-os-starteq.csv`) do **GestãoClick** (sistema atual do Junior). Ouro pra onboarding: puxar histórico dele.
- **Campanhas de reativação** (sazonais + cupom automático) e **etiqueta de OS** (imprimir comanda do aparelho no check-in) — features futuras do ComandaPRO.

4. **SITE (`starteq-palmas`) — NÃO começou nesta sessão.** Frentes do brief: limpar /admin legado (morto), avaliar catalog.ts (provável morto; compatibility.ts ainda serve o montador), página de rastreio (cliente digita code da OS/pedido), imagens reais de produto (hoje SVG), polir. AGENTS.md avisa: Next 16 com breaking changes → espelhar MontadorClient, não inventar API.
4. **Desconto PIX / taxa nas OS:** deixei de fora de propósito (Vendas só). Se adicionar, o `total_cents` da OS TEM que subtrair o desconto (guarda-corpo Palace). Precisa cuidar do schema (OS não tem colunas de cardFee hoje).
5. **Fiscal (NFe):** não começou. Emissor recomendado = Focus NFe (Palmas confirmado). Ver `reference_starteq_emissor_fiscal`.
6. **Offline/PWA:** não começou. Ver `project_starteq_offline_pwa`.
7. **Padrão AgendaPRO (seed de tema no create do tenant):** blinda troca de default global sem tocar tenants. Deferido.

## Mapa de rotas (a testar)
**APIs públicas (middleware):** `/api/loja/*` (produtos GET, pedido POST, montagem POST), `/api/cadastro`, `/api/pedidos`, `/api/delivery-pedido`, `/api/mesa-pedido`, `/api/pontos`, `/api/qz-sign`.
**APIs AT relevantes (auth):** `/api/vendas-peca` POST, `/api/at-pedidos` POST, `/api/os` POST, `/api/cupons` GET/POST, `/api/estoque` GET/POST (+ `/[id]` PATCH/DELETE, `/inventario`), `/api/configuracoes` GET/PUT, `/api/financeiro` GET, `/api/caixa` GET/POST (+ `/historico`), `/api/clientes`, `/api/despesas`(+fixas), `/api/upload-foto`.
**APIs food (não usar no AT, mas compartilhadas — não quebrar):** balcao-venda, vendas, cardapio, cardapio-bar, mesas/*, kds, eventos, garcons, loyalty, fidelidade.
**Telas admin AT:** `/admin` (Início/ATHome), `/admin/os` (+`/[id]`, `/montar`), `/admin/vendas`, `/admin/caixa`, `/admin/estoque`, `/admin/financeiro`, `/admin/cupons`, `/admin/clientes`, `/admin/minha-area`, `/admin/configuracoes`, `/admin/impressora`. (Telas food — balcao/mesas/cardapio/cmv/garcons/eventos/shows/preparo/fidelidade — escondidas por family "food".)

## Regras duras (não escorregar)
- λ.prova-na-fonte: read-after-write no banco; UI verde não é prova.
- λ.financeiro-mostra-o-que-entrou: totalCents (com desconto), nunca subtotal.
- Cor da marca: `app_settings.data.store.primaryColor` (NÃO topo). Cantinho/Medellín vazio de propósito (P&B). Não re-pinar.
- storeId explícito em tudo que roda fora de sessão (senão cai no Cantinho).
- build local (`npx tsc --noEmit`) antes de push; branch + análise de impacto antes de merge na main.
